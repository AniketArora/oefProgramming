#oefening_10
#Vraag aan de gebruiker twee int-getallen n op.
#Bereken nu volgend resultaat: (x + y) * (x + y).
#Print het resultaat af.


getal1 = int(input("Geef het eerste getal in: "))
getal2 = int(input("Geef het tweede getal in: "))

uitkomst = (getal1 + getal2) * (getal1 + getal2)

#print(uitkomst)

print("({0} + {1}) ^ 2) = {2}".format(getal1, getal2, uitkomst))